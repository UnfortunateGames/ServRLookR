[package]
name = "ServRLookR"
version = "0.1.0"
edition = "2024"

[dependencies]
color-eyre = "0.6.5"
crossterm = {version = "*", features = ["event-stream"]}
futures = "0.3.32"
ratatui   = {version = "*", features = []}
tokio     = {version = "*", features = ["full"]}
